# Copy this file to `.env` in the repo root (`.env` is gitignored).
# Android Gradle builds load these values automatically as Dart compile defines.
# For iOS/macOS or `./scripts/flutter`, use the same file.
#
# Must match MEGUCA_CHANCE_UA_SECRET on your Meguca server (awoo.cf).
CHANCE_UA_SECRET=
